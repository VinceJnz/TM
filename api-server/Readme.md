# API server for Trip Manager

## Styling

The single source of truth for shared UI styling is `static/tm-base-theme.css` (served at `/tm-base-theme.css`).

## Styling

The single source of truth for shared UI styling is `static/tm-base-theme.css` (served at `/tm-base-theme.css`).
